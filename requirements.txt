# Runtime dependencies. PyTorch is installed separately by the Windows builder
# so the builder can use the current official Windows wheel / CUDA build.
qwen-asr>=0.0.6
opencc-python-reimplemented==0.1.7
tkinterdnd2>=0.4.3
huggingface-hub[hf_xet]>=0.35
soundfile>=0.13,<0.15
numpy>=2.0,<3
psutil>=6,<8

Pillow>=11,<13
opencv-python-headless>=4.10,<5
